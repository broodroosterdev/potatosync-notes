version: '3'

services:
  db:
    image: 'postgres'
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: notes
      POSTGRES_PASSWORD: notes
      POSTGRES_DB: notes
  
